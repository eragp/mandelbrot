# This is the run configuration file for the mpi runner

# Start one host process
-n 1 ./build/host 
# Start n worker processes. 
# They should use up all available nodes so that one of them
# shares a node with the host
-n 37 ./build/worker
